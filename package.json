{
  "name": "nova-foundry-overlook",
  "version": "5.1.0",
  "private": true,
  "description": "Offline bird’s-eye foundry management game with Energy currency, spaced land plots, facility progression, resettable saves, and world-based navigation",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --target=es2020 --minify --outfile=assets/game.js"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
