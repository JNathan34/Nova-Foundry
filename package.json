{
  "name": "nova-foundry-overlook",
  "version": "5.3.0",
  "private": true,
  "description": "Nova Foundry v5.3 with reliable full progression reset and land-based Energy progression",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --target=es2020 --minify --outfile=assets/game.js"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
