{
  "dependencies": {
    "three": "0.185.1"
  },
  "name": "nova-foundry-3d",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Offline-first 3D reactor incremental game",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --target=es2020 --minify --outfile=assets/game.js"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
